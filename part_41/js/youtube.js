{
    let grades = [1,2,3];
    let poppedValue = grades.pop();
}

// grades.push(n) changes original aray
// grade.pop(n) removes the last value
//unshift adds an element to the beginning of an array
// shift removes the first element of an array
// grades.splice() can be used to add, remove, or replace elements from an array
