{
    var name = prompt();
    
    if (name === "Galen"){ // === is the identity operator
        console.log("Welcome!");
    } else if (name === 'Oberon') {
        console.log("Meow meow meow");
    } else {
        console.log("You're not welcome!");
    }
}

// generalization: program applies to wide range of cases
