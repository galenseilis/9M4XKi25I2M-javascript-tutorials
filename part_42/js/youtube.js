{
    // array.sort() sorts the elements of the array.
    //array.reverse() sorts elements in the reverse order
    // grades.fill(-1, 0, grades.length) replaces values to -1 from indices 0 to grades.length
    //copyWithin() copies a portion of an array to another part of the same array
    
}
