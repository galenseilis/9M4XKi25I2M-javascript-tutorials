{
    let myString = "search this string";
    let charToSearch = "c";
    for (i = 0; i < myString.length; i++){
        if (myString === 'a' || myString === 'e' || myString === 'i'){
            break;
        }
        console.log(mySpring[i]);
    }
}
